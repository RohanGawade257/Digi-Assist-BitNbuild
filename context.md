# Session handoff

Updated 2026-09-11. Resume the current implementation; do not restart or restore the user-deleted architecture folder/master prompt. Preserve unrelated changes, secrets and Docker volumes. Stack remains Next.js/NestJS/shared Zod/Mongo/Firebase. No agents were delegated. Applicable find-docs and UI skills were consulted; Context7 provided current VAD and Sarvam references.

## Current functional outcome

Language activation selects interface/input/reply language, starts a real static Sarvam introduction within the click, requests microphone permission and listens after playback. Focus alone does not activate. Typing remains available. use-voice-assistant.ts owns idle/requesting_permission/listening/finalizing/processing/speaking/paused/error states, local MicVAD v5, one submission after silence, authenticated Sarvam transcription, shared question submission, owned-answer audio/captions, and automatic resumption. Microphone tracks stop during processing/playback. No barge-in is claimed. Pause/Stop/End cancel pending work; End destroys VAD/context and stops capture.

VAD uses self-hosted model/WASM, thresholds .3/.25, minimum450ms speech, leading800ms audio, adjustable2/3.5/5-second silence, and25-second cap with spoken recovery. The previous .65 threshold missed a short Bengali utterance; the default thresholds repaired it while synthetic silence/noise remained rejected. voice-commands.ts uses exact localized commands, including approval; negated/embedded approval phrases do not match. A Bengali repeat spelling error is fixed.

48 pregenerated Sarvam clips under apps/web/public/voice cover intro/recovery and synthetic test questions in six output locales; manifest has text/audio hashes. scripts/generate-voice-assets.cjs only generates fixed public copy using server credentials/quota. No anonymous arbitrary TTS endpoint. Urdu text remains supported with the user-accepted Bulbul v3 output limitation. Manual recording/editable transcription and existing owned-answer controls remain.

Approved-image flow remains capture/upload -> local preview/crop/mask -> explicit exact approval -> question-triggered Gemini inlineData. No manual labels are required. Images remain snapshots, never live-screen claims. Hash, source version, actual PNG dimensions/raster, consent, authentication and request limits are validated. Changed pixels/source require renewed approval. Voice approval cannot succeed after an approval-time version change, sends no image itself, and is followed by another fresh check on questions. A changed/unapproved image causes spoken review. Labels-only/public descriptions remain alternatives; nonvisual review cannot guarantee image privacy. No raw automatic upload or continuous cloud video; no image persistence/logging.

Document PiP preserves captions, status, controls and drafts, with localized separate-click instructions and an opaque side-by-side fallback. Another browser window was foreground during live tests; native OS app focus was not verified. Send/Enter share the guarded path; Shift/IME/drafts remain intact.

## Actual verification

Real browser/Firebase/Sarvam/Gemini synthetic voice-plus-image journeys passed for Hindi, English, Bengali, Marathi and Telugu: two transcriptions200, two answer responses200, two audio responses200, playback progress, automatic resumption and End stopping every captured track. The approved image contains VIOLET COMPASS, absent from manual labels/question; answers read it from pixels.

Tamil: provider chain and an earlier image/audio loop worked, but latest strict accuracy run failed. The short question was misrecognized even when its original uncut WAV went directly to Sarvam, ruling out VAD cutting as the cause. The app correctly requested clarification. Latest image response asked for another view instead of reading the instruction. Do not mark reliable Tamil conversation verified. Urdu: explicit unsupported output shown with zero STT/answer/audio requests. All physical microphone/native-app foreground/human listening/native-language quality checks remain unverified.

Seven final focused browser cases pass (six in a combined run, approval case after fixing test teardown to await End), plus the prior microphone-denial case. Coverage includes silence-pause reset, single submission, playback capture suspension, bounded provider error recovery, exact/negated voice approval, stale-image spoken review, Send/Enter/Shift/IME and retained failed drafts. One focused upstream-timeout unit regression passes. Typecheck, final production builds and docs/OpenAPI checks pass. No broad audit or full old suite was repeated. Earlier image slice had23 core tests,13 integration entries and32 browser tests passing; see docs/validation.md for historical evidence.

Live debugging found upstream20-second timeout returned as408, causing transport replay and duplicate409. Upstream failures now return504. Generation/translation calls allow45 seconds within90-second turns; UI deadline100 seconds. Speech calls remain20 seconds within30-second operations. Cancellation, bounded queue/retries and deduplication remain. All temporary synthetic diagnostics were removed.

## Configuration and safe resumption

Local .env and secrets/firebase-admin.json are valid; never print credentials. User explicitly approved STRICT_PRIVACY_MODE=false for this local instance and API was restarted. Templates remain private by default. In-app voice consent and exact-image approval remain separate. Gemini model gemini-3.5-flash-lite has user-confirmed15RPM/250000TPM/500RPD. Sarvam free/Starter policy is per API: translate60RPM, RESTSTT60RPM, Bulbulv3 TTS30RPM; TPM/RPD explicitly unpublished, not invented. Providers gate independently, same-account keys share quotas/cooldowns, and legacy counters migrate without resetting spend.

Docker project digital-assistant runs web3000/API3001/Mongo27018; do not touch unrelated Mongo27017. Final builds are running at http://localhost:3000. One API replica is supported because cancellation and default-off conversation memory are process-local. Firebase login was verified using exact temporary preverified accounts; normal signup/verification/reset email delivery remains unverified.

scripts/check-live-voice.cjs --run --locale hi-IN uses actual VAD/providers with generated spoken input and synthetic capture. It owns one exact Firebase account/data cleanup and writes test-results/live-voice-LOCALE.json. Each locale has secrets/live-voice-LOCALE-check.json while running; legacy live-voice-check.json also blocks new runs. All completed accounts/data were removed; no pending voice manifests remain. Do not remove unresolved manifests without exact cleanup. Run focused browser tests with --output=test-results/voice-regressions to preserve live JSON evidence; default Playwright output clears the root test-results directory. Fixtures use unique guide_test_UUID databases and exact teardown.

Remaining work: Tamil STT/answer accuracy using native speech, physical microphone and real browser picker, native-app foreground/PiP behavior, human accessibility/language review, normal authentication email delivery and representative-user evaluation. Do not substitute synthetic playback or translated selectors for these gates. README.md/progress.md/docs/validation.md contain the current outcome; older release tables are baseline evidence only.
