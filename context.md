# VaaniSetu session handoff

PiP visual correction: `app/pip.css` clears copied document/root backgrounds and uses an 18% tinted panel without wallpaper or gradients. `FloatingPanel.tsx` fades a separate content layer to 20% after 1.5 seconds outside hover/focus, protecting consent, drafts, errors, editing and Keep visible. Status and Stop speaking remain readable. Explicit minimize/expand and high contrast remain available. Browser screenshots still show a white native backing; desktop transparency is not established.

Current brand is VaaniSetu — Voice-First Digital Access Assistant (`lib/brand.ts`). BrandMark supplies the original voice/bridge icon. `background.ts` centralizes decorative public assets, focal positions, tint and gradient fallback; selected `Assests/bg3.png` has optimized copies prepared by `scripts/prepare-background.py`. `Glass.tsx` + `glass.css` own materials and readable fallbacks. Page high contrast reaches the existing PiP controller without remounting media. Native PiP uses tint only, not the page image or desktop capture. Five intro WAVs were regenerated through real Sarvam and their manifest hashes updated; no new Gemini/provider benchmark. Backend IDs/configuration/accounts are unchanged. Use tests/browser/brand.spec.ts plus existing focused capture/chat/voice checks; screenshots now use vaanisetu-*.png.
Resume the existing application. Do not restore deleted architecture documents, restart broad audits, change secrets or remove Docker volumes. No agents were delegated. This change used the find-docs/UI skills and current Context7 PiP/VAD references.

Current UI: app/redesign.css supplies shared warm-neutral/teal tokens over existing functional styles. Dialog.tsx uses native modal focus containment, Escape and focus restoration; design-copy.ts contains five-language navigation/hero labels. Page owns account/language/help dialogs; AccountTools mode isolates history, feedback and deletion. Main conversation is visible initially, with Screen alongside on desktop and explicit mobile tabs. FloatingPanel transfers the same portal into 390x480 Document PiP; expanded chat scrolls inside its pane. Voice configuration is a dialog. Do not duplicate or remount media controllers when editing layouts. tests/browser/redesign.spec.ts records screenshots and scans rendered accessibility/translated PiP; fixtures now sign in through the Account dialog. No provider changes or new live-provider validation in this redesign. Fifteen focused browser checks passed across final/repair runs (redesign-final, redesign-repair, redesign-voice). Gallery: test-results/redesign-gallery.html. Production web/API/Mongo are healthy; physical microphone/native app-focus validation remains open.
Current languages: Hindi, English, Bengali, Marathi and Telugu. Tamil and Urdu are disabled in selectors, new turn/transcription/preferences validation and provider calls. Historical catalogs remain. Previous real synthetic Firebase/Sarvam/Gemini journeys passed for the five retained languages; current capture tests use provider fixtures. Five fixed spoken consent clips were generated through real Sarvam; this is not new Gemini image evidence.

FloatingPanel owns one real Document PiP window and persistent React portal host. It transfers styles, requests 390x480, offers one explicit action when sharing consumes activation, and preserves controller/media/drafts/review across chat, minimize and window close. Narrow tabs and wider reversible chat/review columns share state. Minimize hides DOM without unmounting. Editing, pending review, questions and errors protect controls. Opaque mode, keyboard/touch reveal, localized status and Stop speaking remain available. The stated fallback is the page beside the target window.

Default capture path: page.captureAndSend -> SafeContext.takeFresh -> captureSource -> LocalCapture.frame -> send(explicit Source) -> shared validation -> Assistant -> Gemini inlineData. The thumbnail uses those same bytes; no React preview/editor state is an upload input. New captureId/time per action, revocable source/session consent, source fingerprint checks, duplicate lock and abort/epoch guards. Strict privacy blocks on-demand image sending. Empty question requests a grounded overview and localized follow-up. No old-mask reuse or continuous uploads. Review before sending remains under More options with frozen crop and exact approval. PiP chat starts closed; main conversation starts visible. Capture/consent speech uses the existing voice controller and preserves an active microphone. Use tests/browser/capture-send.spec.ts for the new default; older review tests need explicit mode selection.
SafeContext freezes images as snapshots in optional review mode. Background changes show status without resetting crops. Refresh/edit invalidates exact-image approval; Approve and answer submits the queued question and exact preview. Label-only freshness is separate. Details remain expandable. Whole-display capture may include the assistant: prefer a target window/tab or move/crop the panel. Hidden content cannot be recovered.

use-voice-assistant owns one microphone/playback controller. Neural frame probabilities feed SpeechDetector (320ms confirmation, 800ms pre-roll, default 2-second silence, 25-second cap). PlaybackEchoGuard compares local PCM with decoded playback, alongside requested browser AEC/noise suppression. Input continues through introduction, processing and answers. Confirmed speech pauses audio, aborts requests (the API propagates HTTP disconnect cancellation) and advances a turn epoch. Late results/callbacks are ignored; interrupted answers are marked. Stop commands do not generate an answer. End clears local detector/reference buffers and all tracks.

Focused evidence is in progress.md and tests/browser/floating-barge.spec.ts. Physical microphone latency/first-word quality, speaker-room echo, native-app focus and whole-display PiP inclusion remain unverified. Synthetic browser playback cannot establish these results; offer headphones for echo problems. Old broad browser/live scripts have previous-layout or retired-language assumptions; adapt selectors before reuse.

Docker: web3000/API3001/project Mongo27018; preserve unrelated Mongo27017. User-authorized STRICT_PRIVACY_MODE=false is already local; voice consent and exact-image approval remain separate. Firebase Admin/.env were previously validated; never print values. Gemini quota: 15RPM/250000TPM/500RPD. Sarvam Starter: translate60RPM/STT60RPM/TTS30RPM, TPM/RPD unpublished; gates independent. One API replica is supported. Normal authentication email delivery and human accessibility/language review remain unverified.

Run focused browser checks with an explicit output directory to preserve other evidence. They start isolated API4101 with unique guide_test_UUID Mongo databases and exact teardown. Never start a second fixture API while one is active. Live scripts own exact temporary-account manifests; never delete unresolved manifests without cleanup.

A read-only cleanup check found four older/unattributed guide_test_* databases. They were left unchanged; current completed fixture runs removed their own manifests/databases. Do not bulk-delete databases based only on their prefix.
